<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="DungeonPartyGame.UI.Pages.SkillTreePage">

    <VerticalStackLayout Padding="20" Spacing="16">
        <Label Text="Skill Trees" FontSize="24" HorizontalOptions="Center" />

        <Label Text="{Binding SelectedCharacter.Name, StringFormat='Skills for {0}'}" FontSize="18" />

        <Label Text="{Binding UnspentSkillPoints, StringFormat='Unspent Skill Points: {0}'}" FontSize="16" />

        <Label Text="Available Nodes:" FontSize="16" />
        <CollectionView ItemsSource="{Binding AvailableNodes}"
                        SelectionMode="Single"
                        SelectedItem="{Binding SelectedNode}">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="10" BackgroundColor="LightBlue">
                        <Label Text="{Binding SkillId}"
                               FontSize="14" FontAttributes="Bold" />
                    </Grid>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <Button Text="Unlock Selected Node"
                Command="{Binding UnlockNodeCommand}"
                IsEnabled="{Binding CanUnlockSelectedNode}" />

        <Label Text="Unlocked Skills:" FontSize="16" />
        <CollectionView ItemsSource="{Binding UnlockedSkills}">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Label Text="{Binding Name}"
                           FontSize="14"
                           BackgroundColor="LightGreen"
                           Padding="5" />
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <Button Text="Back to Main"
                Command="{Binding NavigateBackCommand}" />
    </VerticalStackLayout>
</ContentPage>